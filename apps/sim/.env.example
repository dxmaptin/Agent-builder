# ========================================
# STANDALONE MODE (No Authentication Required)
# ========================================
# Set to true to bypass all authentication and auto-login
# Perfect for single-user, in-house deployments
# STANDALONE_MODE=true
# STANDALONE_USER_EMAIL=admin@localhost
# STANDALONE_USER_NAME=Admin User
# STANDALONE_WORKSPACE_NAME=Default Workspace

# Database (Required)
DATABASE_URL="postgresql://postgres:Xk9$mP7#nQ2@vL5&wR8!zT3^bN6@db.wxtbrpailwudeftuynzp.supabase.co:5432/postgres"

# PostgreSQL Port (Optional) - defaults to 5432 if not specified
# POSTGRES_PORT=5432

# Authentication (Required)
BETTER_AUTH_SECRET=your_better_auth_secret_here  # Use `openssl rand -hex 32` to generate, or visit https://www.better-auth.com/docs/installation
BETTER_AUTH_URL=http://localhost:3000  # Change to your production URL in production

# OAuth Providers (Optional - uncomment and configure as needed)
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret
# GITHUB_CLIENT_ID=your_github_client_id
# GITHUB_CLIENT_SECRET=your_github_client_secret

# JWT Configuration (Optional - for custom token settings)
# JWT_ACCESS_TOKEN_EXPIRY=15m  # Access token lifetime
# JWT_REFRESH_TOKEN_EXPIRY=7d  # Refresh token lifetime
# JWT_ALGORITHM=HS256  # Signing algorithm

# NextJS (Required)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Security (Required)
# CRITICAL: Generate unique values for production - NEVER use these examples!
ENCRYPTION_KEY=your_encryption_key_here  # Use `openssl rand -hex 32` to generate - encrypts sensitive env vars and database fields
INTERNAL_API_SECRET=your_internal_api_secret_here  # Use `openssl rand -hex 32` to generate - secures internal API routes
SESSION_SECRET=your_session_secret_here  # Use `openssl rand -hex 64` to generate - signs session tokens/cookies

# Security Best Practices:
# 1. Rotate these keys every 90 days in production
# 2. Store production keys in secure vault (e.g., AWS Secrets Manager, Vault)
# 3. Never commit actual .env file to version control
# 4. Use different keys for each environment (dev/staging/prod)

# Email Provider (Optional)
# RESEND_API_KEY=  # Uncomment and add your key from https://resend.com to send actual emails
                   # If left commented out, emails will be logged to console instead

# ========================================
# AI/LLM PROVIDER CONFIGURATION (Required)
# ========================================
# At least ONE LLM provider is required for the application to work.
# Choose one or more providers below:

# OpenAI (Recommended for beginners)
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your_openai_api_key_here
# OPENAI_API_KEY_1=  # Optional: Additional keys for load balancing
# OPENAI_API_KEY_2=
# OPENAI_API_KEY_3=

# Anthropic Claude (High quality, great for complex tasks)
# Get your API key from: https://console.anthropic.com/
# ANTHROPIC_API_KEY_1=your_anthropic_api_key_here
# ANTHROPIC_API_KEY_2=  # Optional: Additional keys for load balancing
# ANTHROPIC_API_KEY_3=

# Mistral AI (Cost-effective European provider)
# Get your API key from: https://console.mistral.ai/
# MISTRAL_API_KEY=your_mistral_api_key_here

# Local Ollama (Free, runs on your machine)
# Install Ollama from: https://ollama.com
# OLLAMA_URL=http://localhost:11434

# Azure OpenAI (Enterprise option)
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# AZURE_OPENAI_API_KEY=your_azure_api_key
# AZURE_OPENAI_API_VERSION=2024-02-15-preview
# KB_OPENAI_MODEL_NAME=text-embedding-ada-002  # For knowledge base embeddings

# ========================================
# COPILOT CONFIGURATION (Optional)
# ========================================
# Configure which LLM provider and model to use for the copilot feature
# COPILOT_PROVIDER=openai  # Options: openai, anthropic, ollama
# COPILOT_MODEL=gpt-4o     # Model depends on provider chosen
# COPILOT_API_KEY=         # If different from main API key

# ========================================
# ADDITIONAL AI SERVICES (Optional)
# ========================================
# Text-to-speech for chat
# ELEVENLABS_API_KEY=your_elevenlabs_key

# Web search capabilities
# SERPER_API_KEY=your_serper_key       # Google Search API
# EXA_API_KEY=your_exa_key             # AI-powered search

